name: Scrape Basketball Stats

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests beautifulsoup4 pandas

      - name: Scrape ESPN API (PPG + PPG_L10)
        run: python scrape_espn.py

      - name: Fill missing PPG_L10 from Sports Reference
        run: python add_l10.py

      - name: Commit and push CSV
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin claude/scrape-basketball-stats-LRDNF
          git reset --soft origin/claude/scrape-basketball-stats-LRDNF
          git add march_madness_players.csv
          git diff --cached --quiet || git commit -m "Add scraped March Madness player stats with PPG_L10"
          git push origin HEAD:claude/scrape-basketball-stats-LRDNF
